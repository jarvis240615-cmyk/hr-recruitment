[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = [
  "rm -rf /app/.venv",
  "pip install --no-cache-dir --ignore-installed passlib bcrypt",
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
